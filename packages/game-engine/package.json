{
  "name": "@ll-score/game-engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@ll-score/contracts": "workspace:*",
    "@ll-score/storage-core": "workspace:*"
  },
  "devDependencies": {
    "@ll-score/iam-local": "workspace:*",
    "@ll-score/storage-jsonl": "workspace:*",
    "@types/node": "^22.15.32",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  },
  "dependenciesMeta": {
    "@ll-score/contracts": {
      "injected": true
    },
    "@ll-score/storage-core": {
      "injected": true
    },
    "@ll-score/iam-local": {
      "injected": true
    },
    "@ll-score/storage-jsonl": {
      "injected": true
    }
  }
}
