# @ll-score/field-diagram

Pure field geometry, coordinate conversion, responsive SVG or canvas
primitives, markers, and accessibility helpers shared by pitch location, hit
location, base runners, and replay.

This package owns no feature workflow, game state, API access, or persistence.
