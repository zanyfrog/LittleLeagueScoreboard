# @ll-score/count-controls

Reusable balls, strikes, and outs displays plus fast-entry and correction
controls.

Planned internal structure:

```text
src/model/
src/application/
src/components/
src/index.ts
tests/
```

The package emits typed scoring intents. Game Engine applies count rules.
