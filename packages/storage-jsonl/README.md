# @ll-score/storage-jsonl

Local append-only JSONL implementation of `@ll-score/storage-core`, including
exclusive directory ownership, serialized writes, checksums, projections,
recovery, backup, and restore.
