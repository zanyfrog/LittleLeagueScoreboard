# @ll-score/storage-postgres

Future PostgreSQL adapter for `@ll-score/storage-core`. SQL migrations are
versioned now so JSONL exports have a stable production migration target.
Runtime creation intentionally fails until PostgreSQL is enabled.
