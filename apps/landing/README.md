# ll-score-landing

Next.js PWA and browser-facing request layer. It composes the reusable feature
packages, owns routes and page orchestration, and calls Game Engine services.
It must not access storage repositories directly.
